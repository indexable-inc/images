[package]
name = "prebuilt-lib"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
# A leaf library with no dependencies keeps the produced rlib self-contained,
# so it can be reinjected as a prebuilt unit without dragging in transitive
# dependency units.
