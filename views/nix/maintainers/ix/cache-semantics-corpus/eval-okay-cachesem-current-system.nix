# Unimplemented until the embedder says what platform this is, and the
# platform afterwards. `CURRENT_SYSTEM`'s own comment in eval.rs calls the
# hole this leaves ENG-12541's, and it is: two platforms sharing one memo key
# is a package set built for the wrong machine.
builtins.currentSystem
