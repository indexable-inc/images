[project]
name = "ray-demo"
version = "0.1.0"
description = "Example ix Ray cluster driver"
requires-python = ">=3.13"
dependencies = [
    "ray>=2.55.0,<3.0.0",
]

[project.scripts]
ray-demo = "ray_demo:main"

[build-system]
requires = ["uv_build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
