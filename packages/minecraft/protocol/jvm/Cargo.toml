[package]
name = "mc-protocol-jvm"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "JVM bindings for mc-protocol: Minecraft Server List Ping and format-code stripping over the unibind C-ABI"

[lib]
name = "mc_protocol_jvm"
crate-type = ["cdylib"]

[lints]
workspace = true

[dependencies]
mc-protocol.workspace = true
unibind = { workspace = true, features = ["jvm"] }
unibind-jvm-runtime.workspace = true
