[package]
name = "book"
description = "Minecraft-style book desktop overlay driven by an SQLite file"
version.workspace = true
edition.workspace = true
authors.workspace = true

[[bin]]
name = "book-overlay"
path = "src/main.rs"

[dependencies]
# winit/wgpu/the bitmap font come through overlay-core's re-exports; only the
# domain deps are direct here.
overlay-core.workspace = true
glam.workspace = true
rusqlite.workspace = true
dirs.workspace = true
