[package]
name = "efx-lang"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "The .efx surface language: a total declarative frontend that compiles to efx-ir plans"

[lints]
workspace = true

[dependencies]
efx-ir.workspace = true
snafu.workspace = true

# cargo-unit flattens every workspace test target into one namespace, so
# integration targets carry a crate-unique name.
[[test]]
name = "integration_efx_lang"
path = "tests/lang.rs"
