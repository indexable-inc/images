[package]
name = "dag-complexity-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Graph-agnostic complexity metrics over a DAG: blast radius, critical path, width, leverage, and diff"

[dependencies]
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
