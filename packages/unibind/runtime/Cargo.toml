[package]
name = "unibind-runtime"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Language-free boundary types exported code references at runtime: UniStream"

[lints]
workspace = true

[dependencies]
# `UniStream` itself lives in unibind-stream (see its crate docs for why)
# and is re-exported here.
unibind-stream.workspace = true

[dev-dependencies]
futures.workspace = true
