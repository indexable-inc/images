// Generated by `unibind-gen ts` from the interface embedded in the
// native addon; do not edit.
/** A sample boundary exercising the ts surface. */

import { Buffer } from "node:buffer";
import { z } from "zod";

export const SampleRow = z
  .object({
    id: z.bigint().describe("Identifier."),
    rowLabel: z.string(),
    tags: z.array(z.string()),
    weights: z.record(z.string(), z.number()),
    blob: z.instanceof(Buffer),
    chunks: z.array(z.array(z.number().int())),
    home: z.string().nullable().optional(),
  })
  .describe("A row.");
export type SampleRow = z.infer<typeof SampleRow>;
