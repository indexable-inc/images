//! macOS host agent: presents guest-Linux toplevels as native NSWindows and
//! forwards input back to the guest compositor. See index#1686.

fn main() {
    // Filled in by the host-agent milestone (M3); the stub keeps the cargo
    // workspace resolvable while the crates land in parallel.
    eprintln!("panes-host: not yet implemented (index#1686)");
    std::process::exit(1);
}
