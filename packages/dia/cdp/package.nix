{
  id = "dia-cdp";
  # Launcher for the locally installed Dia.app; macOS-only like packages/dia.
  packageSet = {
    systems = ["aarch64-darwin"];
  };
  flake = {
    systems = ["aarch64-darwin"];
  };
  overlay = {
    systems = ["aarch64-darwin"];
  };
}
