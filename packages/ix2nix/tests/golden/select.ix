const config = { server: { port: 80 } };
const section = "server";

export default {
  direct: config.server.port,
  quoted: config["server"],
  dynamic: config[section],
  fallback: config.server?.host ?? "localhost",
};
