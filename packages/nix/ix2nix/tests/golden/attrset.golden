{ __dir, __importIx }:
let
  defaults = {
    retries = 3;
  };
in
{
  name = "ix";
  dashed-key = true;
  nested = {
    deep = {
      value = null;
    };
  };
  merged = defaults // {
    retries = 5;
    extra = [ 1 2 ];
  };
  ${"key-${defaults.retries}"} = "computed";
}
