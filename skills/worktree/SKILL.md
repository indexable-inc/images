---
description: Work in a Git worktree
---

Non-negotiable:
- Never modify the main checkout
- Main checkout must remain clean and checked out on the main branch at all times
- All changes should be in a worktree at all times
- Create a new Git branch and worktree in `/tmp/<username>/<branch name>` for each new PR
